[project]
name = "chainliterate"
version = "0.1.0"
description = "Teaching you how to build LLM apps with Chainlit."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "asyncpg",
    "boto3",
    "chainlit>=1.0.0",
    "httpx>=0.27",
    "invoke",
    "pytest-asyncio",
    "python-dotenv>=1.0",
    "openai>=1.40.0",
]

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"
